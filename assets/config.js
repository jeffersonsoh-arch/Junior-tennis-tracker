/* Supabase project settings — fill these in from your own project:
   Supabase dashboard -> Project Settings -> API -> Project URL / anon public key.
   The anon key is safe to publish in client code; it only grants what your
   Row Level Security policies allow (see supabase/schema.sql). */
window.DEUCEBOARD_SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
window.DEUCEBOARD_SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
